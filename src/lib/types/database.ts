// ThinkPost AI — Supabase Generated Types
// 
// This file would normally be generated via:
//   npx supabase gen types typescript --project-id your-project-id > src/lib/types/database.ts
//
// For now, we use the manually-defined types in ./index.ts which match
// the FRD Data Dictionary exactly. Once a Supabase project is configured,
// generate this file to get Supabase client type safety.
//
// See SETUP.md for instructions.

export type Json =
  | string
  | number
  | boolean
  | null
  | { [key: string]: Json | undefined }
  | Json[];

// Placeholder — replace with generated types once Supabase project is live.
// The application code uses types from ./index.ts for all business logic.
